import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/sonner"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "SoloBoss AI - Build Your Empire With AI Squad Power",
  description:
    "Command your personal army of 8 specialized AI agents and transform into the legendary boss you were meant to be.",
  keywords: "AI, entrepreneurship, business automation, AI agents, solo entrepreneur",
  authors: [{ name: "SoloBoss AI" }],
  creator: "SoloBoss AI",
  publisher: "SoloBoss AI",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}
